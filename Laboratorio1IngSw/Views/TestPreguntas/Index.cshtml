@model IEnumerable<Laboratorio1IngSw.Models.DB.TestPreguntas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Test.IDTest)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Test.IDTest)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPregunta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPregunta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPregunta })
        </td>
    </tr>
}

</table>
